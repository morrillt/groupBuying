#!/usr/bin/ruby
require File.dirname(__FILE__) + '/../config/environment'

# Called hourly to update the snapshots for a sites active deals

Site.active.reverse.each do |site|
  begin
    site.update_snapshots!
  rescue => e
    puts "Error:"
    puts "-"*90
    puts e.message
  end
end